<!doctype html>
<html lang="tr">
<head>
    <title>Multi-TV</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css">
    <script src="https://unpkg.com/htmx.org@1.7.0/dist/htmx.min.js"></script>
    <link rel="stylesheet" href="/static/styles.css">
</head>
<body>
    <nav class="navbar navbar-dark navbar-expand-lg">
        <div class="container-fluid">
            <a class="navbar-brand" href="#">Multi-TV</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNavDropdown">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            Grid Layout
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdownMenuLink">
                            <li><a class="dropdown-item" href="#" hx-get="/update?channelCount=4" hx-target="#iframeContainer" hx-swap="innerHTML">2x2</a></li>
                            <li><a class="dropdown-item" href="#" hx-get="/update?channelCount=9" hx-target="#iframeContainer" hx-swap="innerHTML">3x3</a></li>
                            <li><a class="dropdown-item" href="#" hx-get="/update?channelCount=12" hx-target="#iframeContainer" hx-swap="innerHTML">3x4</a></li>
                            <li><a class="dropdown-item" href="#" hx-get="/update?channelCount=16" hx-target="#iframeContainer" hx-swap="innerHTML">4x4</a></li>
                        </ul>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
    <div id="iframeContainer" class="iframe-container" ondrop="drop(event)" ondragover="allowDrop(event)">
        {{template "content" .}}
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/static/scripts.js"></script>
</body>
</html>
